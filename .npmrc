# Este proyecto SIEMPRE usa el npm público (no el Artifactory corporativo del PC de
# desarrollo). Así el package-lock.json se mantiene portable y se instala en cualquier
# PC con solo Node.js, sin credenciales privadas.
registry=https://registry.npmjs.org/
