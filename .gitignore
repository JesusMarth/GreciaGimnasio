node_modules/
dist/
data/
# El entorno mock se versiona SOLO la BD (no WAL, ni backups) para tener siempre datos de prueba.
data-mock/*
!data-mock/gymgrecia.db
*.log
.DS_Store
.vite/
# Token de actualizacion (Actualizar.bat). NUNCA se sube: es un secreto y
# GitHub revoca cualquier token que se commitee. Vive solo en el PC del local.
update-token.txt
